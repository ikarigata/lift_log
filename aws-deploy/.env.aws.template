# =============================================================================
# AWS Environment Configuration Template
# =============================================================================
# IMPORTANT: Copy this file to .env.aws and update the values for production
# DO NOT commit .env.aws to version control

# =============================================================================
# マスター設定 (ルート.envと同様の構造)
# =============================================================================
# アプリケーション名
APP_NAME=lift_log
# データベースホスト名 (AWS Docker環境用)
DB_HOST=db

# =============================================================================
# 統合データベース設定 (AWS環境用)
# =============================================================================
# AWS Docker用URL
DB_URL=jdbc:postgresql://db:5432/lift_log

# =============================================================================
# Docker Compose用設定 (PostgreSQLコンテナ初期化用)
# =============================================================================
# PostgreSQLコンテナ作成DB名
POSTGRES_DB=lift_log
# PostgreSQLコンテナユーザー名
POSTGRES_USER=postgres
# PostgreSQLコンテナパスワード (必須変更: 強固なパスワードに設定)
POSTGRES_PASSWORD=CHANGE_THIS_SECURE_PASSWORD

# =============================================================================
# サーバー・アプリケーション設定
# =============================================================================
# Spring Bootサーバーポート
SERVER_PORT=8080
# JWT署名用秘密鍵 (必須変更: 安全な鍵に設定)
JWT_SECRET=CHANGE_THIS_JWT_SECRET
# JWTトークン有効期限（時間）
JWT_EXPIRATION=24

# =============================================================================
# フロントエンド設定
# =============================================================================
# Vite環境変数 (ビルド時にフロントエンドに注入)
# API BaseURL（ルート.envと統一）
VITE_API_BASE_URL=/api/v1
# Mock Service Worker使用フラグ
VITE_USE_MSW=false

# =============================================================================
# 強固なパスワード・シークレット生成コマンド
# =============================================================================
# 使用方法:
# 1. 以下のコマンドでパスワード/シークレットを生成
# 2. 生成された値を上記のCHANGE_THIS_*部分に設定
#
# PostgreSQLパスワード生成:
# openssl rand -base64 24
# 
# JWT秘密鍵生成:
# openssl rand -hex 32