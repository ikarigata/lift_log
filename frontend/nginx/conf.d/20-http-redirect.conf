# ========================================
# HTTP → HTTPS リダイレクト
# ----------------------------------------
# セキュリティのために HTTP アクセスは常に HTTPS へ転送。
# ========================================
server {
  listen 80;
  server_name _;
  return 301 https://$host$request_uri;
}